# useful PIP commands

| commands | Description |
|---|---|
|pip list| lists all the pip packages currently installed |
|python -m pip install --upgrade pip| upgrades the pip to the lastest available version|
| pip install <package> | installs python packages |
| pip install "uvcorn[standard]" | installs standard uvcorn which is a webserver for fast api|
